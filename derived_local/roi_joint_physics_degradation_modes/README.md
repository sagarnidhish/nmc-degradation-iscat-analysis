# Joint ROI Physics Degradation Modes

Combines ROI physics descriptors, front tracking, rollout residuals, residual-CNN guardrail metrics, and cycle-level event/echem evidence.

The resulting modes are automatic hypothesis rankings, not manual annotations or calibrated diffusion coefficients.

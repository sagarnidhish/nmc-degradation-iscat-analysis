# ROI Event-Conditioned Next-Frame Model

PCA-latent ridge next-frame model conditioned on cycle identity, normalized time, and ROI validation score.

Outputs compare teacher-forced and recursive rollout metrics against persistence and velocity baselines, plus rollout residual degradation features.

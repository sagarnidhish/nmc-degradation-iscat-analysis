# Control ROI Selection

Matched non-event reconstructed ROI candidates selected from source movies adjacent to synchronized event cycles.

Use `selected_control_rois.csv` with `tier2_export_selected_roi_sequences.py` to export particle-region control tensors.

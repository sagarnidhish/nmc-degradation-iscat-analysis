# Multi-Cycle ROI Cohort

Automatic event/control ROI cohort for synchronized and single-particle NMC degradation candidate cycles.

Use `multi_cycle_roi_table.csv` with `tier2_export_selected_roi_sequences.py` to export crop tensors.
